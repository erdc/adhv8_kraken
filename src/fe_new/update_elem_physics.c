void update_elem_physics(SSUPER_MODEL *sm, SGRID *grid){

//	after a refinement on the mesh, update the elem_physics structure
// the elem_physics structure is how we can get the global equation orders

//only perform this if something has changed
//or matbe it is the first time step?
if (condition){


}


}